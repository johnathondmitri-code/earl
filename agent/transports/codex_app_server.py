"""Stub — codex_app_server transport stripped (Anthropic only in Earl)."""
from typing import Any
def __getattr__(name: str) -> Any:
    raise AttributeError(f"agent.transports.codex_app_server.{name} — disabled in Earl.")
