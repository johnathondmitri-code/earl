"""Stub — codex_event_projector transport stripped (Anthropic only in Earl)."""
from typing import Any
def __getattr__(name: str) -> Any:
    raise AttributeError(f"agent.transports.codex_event_projector.{name} — disabled in Earl.")
