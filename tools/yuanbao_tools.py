"""Stub — yuanbao_tools stripped from Earl (Telegram-only, Pipedream for integrations)."""
from typing import Any
def __getattr__(name: str) -> Any:
    raise AttributeError(f"tools.yuanbao_tools.{name} — this tool is disabled in Earl.")
