"""Stub — microsoft_graph_client stripped from Earl (Telegram-only, Pipedream for integrations)."""
from typing import Any
def __getattr__(name: str) -> Any:
    raise AttributeError(f"tools.microsoft_graph_client.{name} — this tool is disabled in Earl.")
