"""Stub — homeassistant_tool stripped from Earl (Telegram-only, Pipedream for integrations)."""
from typing import Any
def __getattr__(name: str) -> Any:
    raise AttributeError(f"tools.homeassistant_tool.{name} — this tool is disabled in Earl.")
